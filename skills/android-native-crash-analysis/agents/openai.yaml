interface:
  display_name: "Android Native Crash Analysis"
  short_description: "Deterministic Android NDK crash triage and symbolization"
  default_prompt: "Use $android-native-crash-analysis to analyze an Android native crash with project-aware MCP path resolution, deterministic symbolization, and evidence-driven triage."

policy:
  allow_implicit_invocation: true
