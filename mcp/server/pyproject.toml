[project]
name = "ndktrace-mcp-server"
version = "0.1.0"
description = "MCP wrapper around the ndktrace CLI"
requires-python = ">=3.11"
dependencies = [
  "mcp[cli]>=1.0.0,<2.0.0",
]

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

